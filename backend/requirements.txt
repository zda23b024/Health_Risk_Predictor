Flask>=3.0.0,<4
Flask-Cors>=4.0.0,<5

# Data + ML
numpy>=1.26.0,<3
pandas>=2.2.0,<3
scikit-learn>=1.4.0,<2
joblib>=1.4.0,<2

# Dates (pandas dependencies / compatibility)
python-dateutil>=2.9.0
pytz>=2024.1

# If you truly use pydantic in your project, keep it.
# Remove pydantic completely if you are not using it anywhere.
pydantic>=2.7.0,<3
